# Mini-Task 04 - SB-DB-001

Before running this task:

1. Read `00_SHARED_PREAMBLE.md` and apply it fully.
2. Read this file only if `03_SB_AUTH_TS_001.md` ended with `Gate Decision: PASS`.

Pass-to-next rule:

- If this task ends with `Gate Decision: PASS`, immediately read and execute `05_SB_GATE_001.md`.
- If this task ends with `Gate Decision: FAIL`, stop and report blockers. Do not read the next file.

Prompt:

```text
Task ID: SB-DB-001

Objective:
Ensure terminal build failure paths persist complete metadata, and resolve the DB verification mismatch before claiming PASS.

Context from independent verification:
1. The builder exception fallback did not clearly guarantee finished_at and error_message for every build_failed path.
2. Live runtime evidence created build jobs after March 11, 2026 13:46, but mariadb_nas only saw build_jobs up to March 11, 2026 11:40:20.

Scope:
- backend builder failure metadata
- DB verification alignment
- no unrelated refactors

Requirements:
1. Every terminal build_failed path must persist finished_at, error_message, and updated_at.
2. Investigate why the live runtime build job state and mariadb_nas disagree.
3. If this is a repo or configuration issue, make the smallest fix so runtime and verification target the same MariaDB database.
4. If this is a tool or environment issue outside the repo, do not fake a fix. Report the exact root cause, substitute evidence, and residual risk.
5. Add a focused backend test if needed to cover terminal failure metadata.
6. Preserve the Wi-Fi provisioning flow while fixing DB and terminal-state metadata.

Verification:
1. DB before/after query for a terminal failure path if you can induce one safely.
2. If you cannot induce a real build failure, use the smallest reliable automated test and explain why.
3. Prove whether mariadb_nas and runtime are aligned or still misaligned after your work.
4. Re-check that a Wi-Fi-enabled build job still reaches the verified DB target.
```
